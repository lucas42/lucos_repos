services:
  lucos_repos:
    build: .
    container_name: lucos_repos
    image: lucas42/lucos_repos
    ports:
      - ${PORT}:${PORT}
    environment:
      - PORT
      - SYSTEM
      - ENVIRONMENT
    volumes:
      - data:/data
    restart: always

volumes:
  data:
